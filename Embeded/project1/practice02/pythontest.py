# >>> s = “Hello World”
# >>> p = (“Hong Gil Dong”, 30, ‘M’, [70, 175])
# >>> k = [1, 2, 3, 4]
# >>> d = { ‘name’:’Apple’}
# 가) “Goodbye” + s[-6] + s[6:]
# (                             )
# 나) p[3][1]
# (                             )
# 다) n, a = p[:2]; print a
# (                             )
# 라) print(k[::-1])
# (                             )
# 마) print [3*i for i in k if i > 2]
# (                             )
# 바) d[1] = “Orange”; print(d)
# (                             )